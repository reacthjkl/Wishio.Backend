meta {
  name: CreateWishlist
  type: http
  seq: 2
}

post {
  url: {{apiUrl}}/wishlist
  body: json
  auth: inherit
}

body:json {
  {
    "name": "my test",
    "theme": "0"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
